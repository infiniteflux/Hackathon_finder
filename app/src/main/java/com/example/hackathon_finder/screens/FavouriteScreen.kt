package com.example.hackathon_finder.screens

import androidx.compose.runtime.Composable
import androidx.navigation.NavController
import com.example.hackathon_finder.viewModel.FavouriteViewModel

@Composable
fun FavouriteScreen(navController: NavController, favouriteViewModel: FavouriteViewModel) {
    // this is class still  have to add data on this.
}